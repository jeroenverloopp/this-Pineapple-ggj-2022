using UnityEngine;

namespace Util
{
    public static class Dimensions {

/*
        public static Vector2 TransformPosition2D(Transform transform)
        {
            return new Vector2(transform.position.x, transform.position.z);
        }
        
        public static Vector2 TransformPosition2D(Vector3 position)
        {
            return new Vector2(position.x, position.z);
        }
        
        public static Vector3 TransformPosition3D(Vector2 position, float yPosition)
        {
            return new Vector3(position.x, yPosition, position.y);
        }
        */
        
    }
}